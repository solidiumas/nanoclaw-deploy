services:
  nanoclaw-app:
    image: ghcr.io/solidiumas/nanoclaw-deploy:latest
    container_name: nanoclaw_web
    restart: always
    ports:
      - "80:80"
    # Legg til env-filer eller volumes her hvis appen trenger det
